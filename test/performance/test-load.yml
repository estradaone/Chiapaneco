# tests/performance/test-load.yml
config:
  target: 'angel007.siempredata.net'
  tls:
    rejectUnauthorized: false
  phases:
    - duration: 60
      arrivalRate: 10
scenarios:
  - flow:
      - get:
          url: "/usuarios"
